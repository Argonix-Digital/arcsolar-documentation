---
icon: "helmet-safety"
sidebarTitle: "Field operations"
keywords:
  - "installers"
  - "assignments"
  - "field operations"
  - "roof design"
  - "design handover"
  - "remove installer"
  - "archive installer"
title: "Coordinate field operations and installers"
description: "Prepare the people, work orders and evidence needed on site."
---

**Open:** [**Field Operations**](https://app.arcsolar.com.au/dashboard/field-operations) and [**Installers**](https://app.arcsolar.com.au/dashboard/installers).

## Prepare an installer

Use **Upcoming Work** for planned work orders, **Unassigned** to find work without an installer, **Issues** for blockers and **Completed** for finished work. Open a work order to review its linked customer, project and job before changing it.

Find the existing installer/company before adding another. Review the contact details, active status and qualifications or eligibility information offered by the record. Keep the correct company linked to the job.

## Remove an installer

<Visibility for="humans">
When you stop working with an installer, select their record in [**Installers**](https://app.arcsolar.com.au/dashboard/installers) and choose **Delete**. Confirm with **Confirm delete**.

They are removed from the active installer directory and from assignment options, but historical project and job records stay intact. An installer with active work cannot be removed.
</Visibility>

<Visibility for="agents">
- Bulk removal is the **Delete** button and opens **Remove N installer(s)?** — "The selected installer(s) will be removed from active directories and assignment options. Historical project and job records will remain intact. Installers with active work cannot be removed."
- The confirm action is **Confirm delete** (in flight, **Removing…**). Success toasts are "Installer removed" / "Installers removed" — "N installer records are no longer active."
- Describe this as removal from active directories, not as deleting the installer's history. Do not present it as available while the installer has active work.
</Visibility>

## Prepare field work

1. Find the project and job.
2. Open or create the appropriate work order using the page controls.
3. Confirm the scope, site, installer, date and materials.
4. Provide the available site documents and access notes.
5. Review outstanding readiness conditions.
6. Confirm the work order and check its saved status.

## What the installer receives

The approved design reaches the installer as part of the issued quote, not through a separate tool. They see the same two layouts the customer approved — the **Solar panel layout** and the **Stringing and component layout** — on the issued quote and its PDF, alongside the project and the work order for the job.

If a job needs more, the **design editor** can export installer-oriented documents: a single line diagram PDF and the roof reports. Those come from the editor, not from ArcSolar, and they are preliminary design outputs stamped **Preliminary design — verify on site**, not certification.

There is **no separate installer design screen** in ArcSolar. Installers do not draw, edit or approve a design here. If the site differs from what the design shows, record the difference against the job and raise the next action rather than editing the design.

<Visibility for="agents">
  The design surface is deliberately rep- and customer-facing only. `projects/` and `field-operations/` contain no roof design reference, and there is no installer route group for design. The installer-facing design artefacts inside ArcSolar are the issued estimate and the PDFs it produces, and nothing else. Do not describe an installer design view, an installer SLD, or design editing by an installer. Full detail: [Roof design](/quoting/roof-design) and the [Roof design contract](/reference/roof-design-contract).

  **Which renderer produces which PDF.** The **estimate PDF** (`src/lib/quotes/estimate-pdf.ts`) builds the two drawings as pdfmake content inside a section headed **PROPOSED ROOFTOP DESIGN**, captioned **PROPOSED PANEL LAYOUT** and **PANEL STRINGS & COMPONENT MARKINGS**. The **contract PDF** (`contract-pdf.ts`) and the **tenant-document PDF** (`tenant-document-pdf.ts`) append one A4 landscape page per image through `appendRoofDesignPages`. Do not attach `appendRoofDesignPages` to the estimate PDF.

  **What the design editor itself can hand to an installer.** The editor, not ArcSolar, produces the installer-oriented downloads: the A4-landscape **project sheet** single line diagram PDF from **Download SLD PDF**, plus the roof reports **Panel Design**, **Annotation Design**, **Site Plan**, **Battery Site Plan** and **Voltage Rise Calculation**, each footer-stamped `Roof Studio · <date> · Preliminary design — verify on site`. A fixed **A3** installation sheet exists in the editor's code but is not reachable from its interface. These are preliminary design outputs, not certification — the voltage rise report states `No reactance, thermal cable-capacity, protection or regulatory compliance assessment is included.` and `The entered comparison limit is a design input, not a certification of compliance.`

  Both statements are true together: ArcSolar has no installer design surface, and the design editor can export installer-oriented documents. Neither product has an installer login, role or workspace.
</Visibility>

## During and after the visit

Record the outcome and missing items. Attach required evidence through the supported file controls. Keep installation, inspection, grid/metering and rebate progress separate.

If an installer cannot complete the job, record the reason and arrange an owned next action. Do not use completion as a substitute for a revisit or missing compliance document.

## ArgonixIntelligence in this workflow

Use customer and job context to prepare the handover. Intelligence does not establish installer accreditation, site safety or compliant completion.
